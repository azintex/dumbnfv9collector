# Collector mode will be 'raw' or 'unpack'.
# 'unpack' mode is default.
mode = 'unpack'

# Listen IP address.
ip_address = '172.16.93.5'

# Listen port (UDP).
port = 2055

# Template size in bytes. Must be same size as template on exporter.
template_size = 50

# Capturing duration in seconds
capturing_duration = 300
